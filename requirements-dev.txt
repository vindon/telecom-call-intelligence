# Development dependencies — install with: pip install -r requirements-dev.txt
# Production deps are in requirements.txt

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0        # coverage reports
pytest-mock>=3.14.0      # MagicMock fixtures

# Linting and formatting
ruff>=0.4.0

# Type checking
mypy>=1.10.0

# Pre-commit hooks
pre-commit>=3.7.0
