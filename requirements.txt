# This is the abstract dependency spec — human-edited, loose >= bounds.
# Installs (local, CI, Render) use requirements.lock, the fully pinned
# resolution generated from this file. After editing this file, run
# `make lock` to regenerate requirements.lock before committing.

# Google AI Studio inference (Gemini 2.0 Flash — free tier)
# NOTE: google-generativeai is deprecated; use google-genai
google-genai>=1.0.0

# NVIDIA NIM API (OpenAI-compatible) — InsightsAgent primary provider
openai>=1.0.0

# Anthropic Claude — InsightsAgent fallback
anthropic>=0.40.0

# LangGraph orchestration + LangSmith tracing
langgraph>=0.2.0
langchain-core>=0.3.0
langsmith>=0.1.0

# Langfuse observability (pipeline/tracing.py) — optional, no-op without
# LANGFUSE_PUBLIC_KEY/SECRET_KEY set. Auto-instruments Claude (Anthropic)
# and Gemini (google-genai); NVIDIA NIM is covered by langfuse's own
# OpenAI-SDK wrapper (no extra package — see llm_clients.get_nvidia_client()).
langfuse>=3.0.0
opentelemetry-instrumentation-anthropic>=0.40.0
openinference-instrumentation-google-genai>=0.1.0

# HuggingFace dataset loading
datasets>=2.20.0
huggingface-hub>=0.23.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Dashboard
streamlit>=1.35.0
plotly>=5.22.0

# FastAPI wrapper
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
